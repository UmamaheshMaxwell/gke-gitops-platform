# Bootstrap

Initial setup scripts and configuration for provisioning the platform foundation.

## Purpose

Bootstrap resources that must exist before the main infrastructure pipeline can run, such as:

- Remote state backend (Cloud Storage bucket)
- IAM roles and service accounts for Terraform
- Initial GCP project configuration

## Status

Work in progress — bootstrap scripts will be added in Phase 1 (Foundation).
